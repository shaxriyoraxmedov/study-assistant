# Репозиторий разрабатывается на Windows, а собирается в Linux-контейнере и на
# CI-раннере. Без этого файла Git подставлял бы CRLF, и shell-скрипты внутри
# образа ломались бы на \r.
* text=auto eol=lf
*.png binary
*.jpg binary
